<div id="task-frame" class="task-frame">
  <h3 class="task-frame__heading">Task <%= task_id %> failed</h3>
  <div class="alert alert--error">
    <p><%= message.presence || "Please try again in a moment." %></p>
  </div>
  <p class="task-frame__hint">Close and reopen the modal to retry.</p>
</div>
