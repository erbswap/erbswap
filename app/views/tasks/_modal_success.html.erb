<div id="task-frame" class="task-frame">
  <h3 class="task-frame__heading">Task <%= task_id %> complete</h3>
  <div class="alert alert--success">
    <p>Returned <%= pluralize(item_count, "item") %>.</p>
  </div>
  <p class="task-frame__hint">Close and reopen the modal to start over.</p>
</div>
